# Security policy

このGitHub PagesリポジトリはPublic Repositoryであり、保存したファイルは第三者から閲覧できることを前提とする。

## 保存してはいけない情報

- APIキー、アクセストークン、パスワード
- サービスアカウント鍵、OAuthクライアントシークレット
- FirebaseやGoogle Cloudの秘密情報
- Analyticsのユーザー単位・イベント単位の生データ
- 公開を意図していない環境固有の設定値

## `products/`ディレクトリの運用

`products/`以下には、Webサイトとして公開するために必要なHTML、CSS、JavaScript、画像、公開用JSONなどの静的ファイルだけを配置する。

シェルスクリプト、Pythonコード、テストコード、データ生成ツール、運用・管理ツールなど、Webサイトの公開物ではないファイルを`products/`以下へ配置してはならない。これらのツールと環境固有設定は、それぞれのアプリ本体リポジトリで管理する。

公開用JSONには公開を意図した集計済みデータだけを含め、ユーザー識別情報、端末情報、プレイヤー名、カード内容などを含めてはならない。

コミット前に差分を確認し、認証ファイルや秘密情報が含まれていないことを確認する。
